<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://unpkg.com/material-components-web@v4.0.0/dist/material-components-web.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../styles/btnNeon.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Rubik+Mono+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="../styles/result.css">
</head>

<body>
    <div class="wrapper">
        <h1 class="resultsh1">Результаты</h1>
        <h1 class="neon-heading first"><span class="flickering-3">Р</span>ез<span class="flickering-1">у</span>льт<span class="flickering-3">а</span> <span class="flickering-2">т</span><span class="flickering-3">ы</span></h1>
        <div class="timer"></div>

        <div class="results__chart">
            <% membersRes.forEach(member => { %>
                <div class="progress">
                    <div class="progress-bar" role="progressbar" style="width: <%= isNaN(member[1] * 100 / votes) || !isFinite(member[1] * 100 / votes)  ? 20 : (member[1] * 100 / votes)  %>% " aria-valuenow="100" aria-valuemin="0" aria-valuemax="100">

                    </div>
                    <p class="progp">
                        <%= member[0].split('_').join(' ') %>
                            <%= isNaN(member[1] * 100 / votes) || !isFinite(member[1] * 100 /votes)  ? 0 : (member[1] * 100 / votes)  %>%
                    </p>
                </div>
                <% });%>



        </div>
        <form action="/newvote" method="post" id="createForm"></form>
        <form action="/finish" method="post" id="finishForm"></form>
        <div class="control">


        </div>
    </div>
    <script src="/scripts/cookies.js"></script>
    <script src="/scripts/result.js"></script>
</body>

</html>